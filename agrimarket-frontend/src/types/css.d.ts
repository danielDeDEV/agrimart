// Stylesheets are handled by Next.js, not TypeScript. This declaration lets
// `import './globals.css'` type-check under TypeScript 6+, which reports
// side-effect imports it cannot resolve (noUncheckedSideEffectImports).
declare module '*.css';
