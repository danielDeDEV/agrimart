NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
# This website's own public address. Used for the sitemap, share cards and
# canonical links, and baked in at build time — set it before npm run build.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_USSD_CODE="*920*1234#"
# Leave empty unless the gateway gives you an SMS short code for inbound keywords
NEXT_PUBLIC_SMS_SHORTCODE=
NEXT_PUBLIC_SUPPORT_PHONE=0302 000 000
NEXT_PUBLIC_SUPPORT_EMAIL=support@agrimart.gh
